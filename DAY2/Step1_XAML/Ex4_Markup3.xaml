<Window x:Class="Step1_XAML.Ex4_Markup3"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Step1_XAML"
        mc:Ignorable="d"
        Title="Ex4_Markup3" Height="450" Width="800">
    <StackPanel>

        <!-- 사용자 정의 makrup 객체 생성시, 속성을 전달하는 코드 -->
        <Button Content="확인" FontSize="{local:FontInfo Element=Title, Key=FontSize}" 
                               Background="{local:FontInfo Element=Title, Key=Background}"/>



        <Button Content="확인" FontSize="{local:FontInfo Element=SubTitle, Key=FontSize}" 
                               Background="{local:FontInfo Element=SubTitle, Key=Background}"/>
        
    </StackPanel>
</Window>
